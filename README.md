# React JSX Exam (Scaffolded)

Minimal React 18 + Vite project scaffold.

Scripts:

- npm run dev  - start dev server
- npm run build - build for production
- npm run preview - locally preview production build
